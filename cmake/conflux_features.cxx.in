// Auto-generated by CMake — do not edit.
// Source template: cmake/conflux_features.cxx.in
export module conflux.features;

export namespace conflux {
    // true when the library was built with OpenSSL (TLS + JWT crypto).
    inline constexpr bool HAS_TLS      = @CONFLUX_HAS_TLS@;
    // true when the library was built with at least one gzip-capable backend.
    inline constexpr bool HAS_COMPRESS = @CONFLUX_HAS_COMPRESS@;
    // true when the library was built with the classic zlib gzip backend.
    inline constexpr bool HAS_ZLIB = @CONFLUX_HAS_ZLIB@;
    // true when the library was built with libdeflate.
    inline constexpr bool HAS_LIBDEFLATE = @CONFLUX_HAS_LIBDEFLATE@;
    // true when the library was built with zlib-ng.
    inline constexpr bool HAS_ZLIB_NG = @CONFLUX_HAS_ZLIB_NG@;
    // true when the library was built with ISA-L igzip.
    inline constexpr bool HAS_ISAL = @CONFLUX_HAS_ISAL@;
    // true when the library was built with brotli.
    inline constexpr bool HAS_BROTLI   = @CONFLUX_HAS_BROTLI@;
    // true when the library was built with zstd.
    inline constexpr bool HAS_ZSTD     = @CONFLUX_HAS_ZSTD@;
    // true when the native JSON parser/DOM provider is compiled in.
    inline constexpr bool HAS_JSON = @CONFLUX_HAS_JSON@;
    // true when inotify-backed file watch support is compiled in.
    inline constexpr bool HAS_FILE_WATCH = @CONFLUX_HAS_FILE_WATCH@;
    // true when the template engine module is compiled in.
    inline constexpr bool HAS_TEMPLATES = @CONFLUX_HAS_TEMPLATES@;
    // true when the template watcher adapter is compiled in.
    inline constexpr bool HAS_TEMPLATES_WATCH = @CONFLUX_HAS_TEMPLATES_WATCH@;
    // true when Prometheus metrics support is compiled in.
    inline constexpr bool HAS_METRICS  = @CONFLUX_HAS_METRICS@;
    // true when HTTP/2 support is compiled in (requires nghttp2 + TLS).
    inline constexpr bool HAS_HTTP2    = @CONFLUX_HAS_HTTP2@;
    // true when HTTP/3 support is compiled in (requires ngtcp2 + nghttp3 + OpenSSL 3.5 + TLS).
    inline constexpr bool HAS_HTTP3    = @CONFLUX_HAS_HTTP3@;
    // true when the PostgreSQL client module is compiled in (requires libpq).
    inline constexpr bool HAS_DB       = @CONFLUX_HAS_DB@;

}
