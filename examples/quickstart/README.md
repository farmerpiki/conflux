# Quickstart examples

These examples show the first-contact HTTP app facade. They intentionally import
only `conflux.http` from Conflux modules and avoid lower-level runtime, provider,
and raw response APIs.

Use `examples/advanced/` for explicit providers, work pools, raw DB/runtime
setup, HTTP/3, and other lower-level integration paths.
