# conflux.work Migration Inventory

Generated by Phase 0 grep sweep. Updated by each E1.2 PR when callsites in a
group are migrated (before/after pairs go in the per-group file under
`docs/migration/<group>.md`).

**Symmetric rule**: any addition or deletion of a public symbol in E1–E5 also
updates `tests/work_api_snapshot.cxx`.

---

## Legacy types targeted for migration (E1.0 – E1.4)

| Type / Symbol | Replacement | Phase |
|---|---|---|
| `work_detail::Flow<T>` | `conflux::work::root::Task<T>` | E1.0 |
| `work_detail::FlowSource<T>` | `conflux::work::root::TaskSource<T>` | E1.0 |
| `work_detail::FlowAwaiter<T>` | removed (internal) | E1.4 |
| `task_as_flow(root::Task<T>&&)` | `co_await std::move(task)` directly | E1.4 |
| `conflux::work::Task<T>` (Flow-backed) | `conflux::work::root::Task<T>` | E1.0 |
| `work_detail::UniqueFn<Sig>` | `std::move_only_function<Sig>` (public) / `detail::small_move_only_function<Sig,32>` (internal) | E2a |
| `conflux::work::BufferView<T>` | `std::span<T const>` | E2a |
| `conflux::work::carrier::model_b::*` | removed (no replacement; model_a is canonical) | E1.4 |
| `struct ValueTag` / `value{}` pipe tag | removed | E1.4 |
| `ThenStep`, `FlatThenStep`, `ErrorStep`, `CancelStep`, `MoveToStep`, `StartOnStep` | `Chain<T>` combinators (model_a) | E1.4 |
| `source.commit_success/commit_failure/commit_cancelled` | `source.try_set_value/try_set_exception/try_set_error/try_set_cancelled` | E4 |

---

## Callsite inventory

### examples/

No legacy type usage found.

### tests/

#### `tests/work_carrier_test.cxx` — model_b (69 occurrences)

| Line | Usage |
|------|-------|
| 8 | `import conflux.work.carrier.model_b;` |
| 12 | `namespace model_b = conflux::work::carrier::model_b;` |
| 311–548 | All test cases tagged `[carrier.model_b]` using `model_b::from_task`, `model_b::map`, `model_b::then`, `model_b::hop_to_posted`, `model_b::hop_to_operation`, `model_b::when_all` |

Migration: wrap with `[[deprecated]]` at E1.1; delete at E1.4.

#### `tests/http_e2e.cxx` — `set_value` (4 occurrences)

| Line | Usage |
|------|-------|
| 6781 | `result->set_value(false);` |
| 6782 | `} catch (std::invalid_argument const &) { result->set_value(true); }` |
| 6800 | `result->set_value(false);` |
| 6801 | `} catch (std::invalid_argument const &) { result->set_value(true); }` |

Note: these are `std::promise::set_value`, not `Source::set_value`. Not a migration target.

### src/net/

#### `src/net/http_server.cxx` — `UniqueFn` (2 occurrences)

| Line | Usage |
|------|-------|
| 730 | `deque<work_detail::UniqueFn<void()>> pending_ops{};` |
| 1478 | `work_detail::UniqueFn<void()> op)` |

Migration: `std::move_only_function<void()>` at E2a.

#### `src/net/dns/dns.cxx` — comment reference (1 occurrence)

| Line | Usage |
|------|-------|
| 1785 | Comment mentioning `FlowSource` — update wording at E1.3. |

### src/file_io/

#### `src/file_io/file_io.cxx` — comment reference (1 occurrence)

| Line | Usage |
|------|-------|
| 530 | Comment mentioning `Flow<T>` pipelines — update wording at E1.3. |

### benchmarks/

#### `benchmarks/work_bench.cxx` and `benchmarks/work_compile_bench.cxx` — pre-guarded

All model_b code in both bench files is already gated behind
`#if CONFLUX_WORK_CARRIER_MODEL_B` (default 0). No CMakeLists.txt target
sets this flag, so model_b bench cases are already excluded from compilation.
No migration action required — will be deleted at E1.4.

### src/db/

No legacy type usage found.

### src/work/ (internal — not migration targets, tracked for deletion at E1.4)

All `Flow<T>`, `FlowAwaiter<T>`, `FlowSource<T>`, `task_as_flow`, `UniqueFn`,
`ThenStep`, `FlatThenStep`, `ErrorStep`, `CancelStep`, `MoveToStep`,
`StartOnStep`, `ValueTag` definitions live in `src/work.cxx` and
`src/work/carrier_model_b.cxx`. These are the definitions being deprecated
and deleted, not callsites.

---

## Status per group

| Group | Status | PR |
|---|---|---|
| examples | done — no legacy usage | — |
| tests (model_b / work_carrier_test) | done E1.2 — model_b section removed | — |
| net (http_server) | done E2a — UniqueFn replaced with small_move_only_function | — |
| net (dns comment) | done E1.3 — FlowSource → TaskSource in comment | — |
| file_io (comment) | done E1.3 — Flow<T> pipelines → Task<T> chains in comment | — |
| db | done — no legacy usage | — |
