# Release Evidence Template

Commit:

Preset:

Compiler and standard library:

CMake and Ninja:

Kernel and container notes:

Interface mode:

Package components requested:

Commands run:

Pass/fail summary:

Skipped checks and reason:

Benchmark artifact paths, if any:
