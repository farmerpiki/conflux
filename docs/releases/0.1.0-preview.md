# Conflux 0.1.0 Preview

Status: first public-preview release notes.

## Scope

Preview package components:

- `core`
- `json`
- `file_io_sync`
- `http`, only from real-liburing builds with runtime evidence attached
- `work`, only from real-liburing builds with runtime evidence attached

Preview SKUs:

- `release-json`: `core`, `json`, `file_io_sync`
- `release-http-api`: `core`, `json`, `http`
- `release-web-server`: `core`, `json`, `http`
- `release-pg`: `core`, `json`, `pg` (requires libpq; PostgreSQL integration and
  pipeline evidence is now attached)

Recommended producer feature bundles:

- `core`
- `json`
- `http-minimal`
- `http-api`

Experimental or conditional components and bundles should not be advertised as
the default entry point for first-contact users: HTTP/2, HTTP/3, P2996
reflection, SEND_ZC tuning, and the full `complete` aggregate surface.
PostgreSQL pipeline support may be advertised with `pg` only when the
PostgreSQL integration and pipeline evidence run is attached and green.

## Platform

Conflux is Linux-only and `io_uring`-first. A usable `io_uring` runtime and
`liburing` development package are required for runtime-facing components,
including HTTP server/client components, async file I/O, socket I/O, and the
tests and benchmarks that exercise those surfaces. The `core`, `json`,
`file_io_sync`, and `file_map` component surfaces remain liburing-free.
Liburing-free installs advertise only dependency-light components for generated
header artifact checks and intentionally do not publish runtime-facing
components such as `work` or `http`.

Conflux is modules-first. C++ module consumption is toolchain-sensitive, so use
the checked-in CMake presets as the support matrix for this preview. The
intended preview floor is GCC 15, GCC 16, Clang 21, CMake 4.2+, and Ninja; do
not treat arbitrary compiler/version combinations or older CMake versions as
supported unless they configure, build, and pass the matching tests in release
evidence. GCC 15 is the no-LTO release lane; GCC 16 and Clang 21 are the
LTO-capable release lanes; GCC 16 is the current P2996 reflection lane.

Container or seccomp policies can block runtime setup even on a new kernel.
Runtime capability probes and test results are the release evidence source of
truth.

## Install And Consume

The prerelease primary source-consumption mode is `MODULE_INTERFACE`:

```cmake
find_package(conflux REQUIRED COMPONENTS http json)
target_link_libraries(app PRIVATE conflux::http conflux::json)
```

Generated headers are staged release artifacts for header-mode consumers that
cannot use modules and are generated from the same module sources. Header mode
is supported only for the components and toolchains covered by
package-smoke evidence; other uses are best-effort:

```cmake
find_package(conflux REQUIRED COMPONENTS core json file_io_sync)
target_link_libraries(app PRIVATE conflux::core conflux::json conflux::file_io_sync)
```

## Supported Components

- Liburing-free install: `core`, `types`, `json`, `file_io_sync`.
- real-liburing install: may add runtime-facing package components such as
  `work` and `http`.
- DB-off install: no generated DB headers and no `db` / `pg` package contract.
- DB-on install: `pg` only when libpq is available and DB evidence is attached.
  `db` is not an advertised preview package component.

## License

Conflux is licensed under the Apache License, Version 2.0. See `LICENSE` and
`NOTICE`.

## API Stability

This is a pre-v1 preview. APIs may still break before v1. Public examples and
docs should use final preview names only. Deprecated compatibility aliases are
not part of the advertised surface.

## Evidence

Release evidence is in the `conflux_evidence` repository.

**Evidence repo:** https://github.com/farmerpiki/conflux_evidence  
**Evidence commit:** `d2bcbfb` (produced 2026-06-09)
**Conflux commit:** `f0f511b6159829b3d5ff0f6217a85a76fc1110e9` (`v0.1.0-preview`)

What is attached:

- Full SKU closures for release-json (full ASAN/UBSAN/TSAN), release-http-api, and
  release-web-server; source archives and generated header artifacts for each.
- JSON parser conformance: JSONTestSuite accept/reject + differential smoke
  (node + python3), clang 21.1.8.
- JSON throughput benchmarks: 6 rounds per compiler, 3 compilers (clang 21.1.8,
  g++ 15.2.1, g++-16 16.1.0 with import std).
- HTTP throughput supporting measurements: 4 workloads (plaintext GET, small-JSON GET,
  route-param GET, echo-POST) against uWebSockets, Boost.Beast, libmicrohttpd,
  libevent; 6 rounds per server; AMD Ryzen 7 5800X. Raw HTTP artifacts are
  recorded under source-equivalent commit `6466c3cb`; use the evidence
  README/metadata as the source of truth for the exact benchmark commit and
  scope.
- Docker conformance: h2spec 2.6.0 (HTTP/2), Autobahn wstest (WebSocket),
  spectral 6.16.0 (OpenAPI). Raw artifacts at `conflux_evidence/conformance-docker/`.

Not attached in this preview:

- PostgreSQL integration/pipeline evidence (`pg` is not advertised for this preview).
- HTTP/2 multiplexing, WebSocket throughput, static file, large body benchmarks.
- JSON benchmark public proof (current JSON numbers are supporting measurements,
  not formal benchmark-schema proof).

## Migration Notes

Public examples and first-contact docs use final preview spellings. Historical
short aliases and removed async/sync names are tracked in migration docs; they
are not promoted as the preview API.

## Known Limitations

- Linux only.
- Runtime-facing and HTTP packages require real liburing.
- C++ module consumption is primary and toolchain-sensitive.
- Supported toolchains are intentionally narrow and tied to release evidence.
- Generated headers are header-mode artifacts, not hand-maintained source or
  the design center for new APIs.
- Optional protocol and DB support depends on detected system libraries.
- Exact-commit formal HTTP benchmark proof, HTTP/2 multiplexing, WebSocket
  throughput, static file, and large body benchmarks are not in the current
  evidence set; see `conflux_evidence` for what is attached.
- Formal JSON benchmark proof (full benchmark schema) is not in this preview;
  current JSON numbers are supporting measurements. public JSON benchmark proof
  with full benchmark-schema is not attached.
- Third-party protocol conformance (h2spec/wstest/spectral) is via Docker; native
  host tool conformance is not attached. external OpenAPI validator results are
  from spectral in Docker (0 errors, 5 warnings on optional metadata fields).
- Full JSONTestSuite classification and parser differential testing are attached
  for the JSON lane (clang 21.1.8).
- PostgreSQL integration evidence is attached for `release-pg`; `pg` is advertised
  for builds with libpq available. `db` is not an advertised preview package component.
- Generated header release artifacts are staged via `stage-release-artifacts.sh`.

## Benchmark Claims Policy

Do not make performance claims without same-machine benchmark artifacts. Release
notes may cite benchmark artifact paths after the final benchmark run is
attached, but raw bulky logs stay outside the tracked source tree. Benchmark
graphs should be generated from that final artifact set just before tagging so
they reflect the released source state. If a measurement is from a
source-equivalent commit, name both commits and state why it is relevant;
otherwise do not call it exact-release benchmark evidence.

## Readiness history

The prerelease readiness branch previously landed these acceptance checks:

- `python3 scripts/check_no_std_streams.py` passed after reusable-source stderr
  diagnostics moved to internal sinks or low-level `write(2)` where target
  coupling required it.
- MODULE_INTERFACE is the primary preview interface; generated HEADER_INTERFACE
  artifacts are header-mode output from the module source.
- HEADER_INTERFACE DB-off artifact configure/build passed with
  `CONFLUX_BUILD_TESTS=OFF`, `CONFLUX_BUILD_BENCHMARKS=OFF`, and
  `CONFLUX_POSTGRES_PROVIDER=OFF`.
- DB-off install pruned `conflux/db*` and `conflux/pg*` generated headers.
- Installed artifact package smoke passed for liburing-free
  `core;json;file_io_sync` in HEADER_INTERFACE mode.
- DB-enabled HEADER_INTERFACE build passed when libpq was available.
- HEADER_INTERFACE artifact test configure/build passed with system Catch2; CTest
  reported the registered compile/package gates green for that lane.

Active gates moved forward to `docs/release-checklist.md`: modules-first
source/package smoke, generated-header artifact staging, DB-off component pruning, no-standard-stream checks,
first-contact public spelling checks, package component matrix smoke, and
always-on example compilation.
