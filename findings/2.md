Excluded tests, fuzz, benchmarks, examples. Static scan only; no patch generated.

## P0 — worth fixing

* **DONE: Unify Authorization parsing**

  * Duplicates:

    * `src/net/auth.cxx:65` — `auth_detail::credentials_for_scheme`
    * `src/net/app_route_helpers.cxx:183` — `detail::credentials_for_scheme`
    * `src/net/jwt.cxx:274` — `jwt_detail::bearer_token`
    * call sites in `src/net/app.cxx:178`, `src/net/app.cxx:1687`
  * Issue: same Bearer/Basic parsing has slightly different whitespace behavior.
  * Use one internal helper, e.g. `conflux.net.auth_header`.
  * Value: correctness + less duplicated auth-path logic.

* **Move no-allocation SHA256/HMAC internals out of password hashing**

  * Better internals already exist:

    * `src/net/password_hash.cxx:437` — SHA256 state/compress/update/final
    * `src/net/password_hash.cxx:547` — `sha256_noalloc`
    * `src/net/password_hash.cxx:554` — reusable `HmacSha256Key`
    * `src/net/password_hash.cxx:583` — `hmac_sha256_noalloc`
  * Public crypto path allocates:

    * `src/crypto.cxx:241` — `sha256`
    * `src/crypto.cxx:320` — `hmac_sha256`
  * Allocation-heavy callers:

    * `src/net/cookie_signing.cxx:21`
    * `src/net/jwt.cxx:294`, `src/net/jwt.cxx:493`
    * `src/net/auth.cxx:494`
  * Move SHA/HMAC core to crypto detail internals; make public crypto + JWT + cookie signing + password hash share it.
  * Value: real hot-path perf win, less crypto duplication.

* **DONE: Centralize JSON Content-Type predicates**

  * Repeated checks in:

    * `src/net/app.cxx:1613`
    * `src/net/app.cxx:1628`
    * `src/net/app.cxx:1645`
    * `src/net/app.cxx:1660`
    * `src/net/app.cxx:2162`
  * Current `starts_with("application/json")` shape can accidentally accept `application/jsonp`.
  * Add internal helpers:

    * `content_type_is_json`
    * `content_type_is_problem_json`
    * `content_type_is_json_patch`
    * `content_type_is_merge_patch`
  * Put them near `src/net/app_json_helpers.cxx`.
  * Value: correctness/security + less copy-paste.

* **DONE: Share HTTP client response-head parsing**

  * Request wire is already shared through `client_wire`.
  * Duplicated response-head parsing:

    * `src/net/client.cxx:701`
    * `src/net/client_async_impl.cxx:628`
  * Extract into `client_wire`, returning parsed status, headers, content-length, chunked flag, cookies.
  * Value: avoids sync/async client drift; CL/TE parsing bugs become one fix.

## P1 — good cleanup / drift prevention

* **DONE: Use existing HTTP whitespace trim helper**

  * Existing:

    * `src/net/http_types.cxx:357` — `conflux::http::trim_http_whitespace`
  * Duplicates:

    * `src/net/router_impl.cxx:279` — `trim_ascii_ws`
    * `src/net/app_route_helpers.cxx:173` — `trim_ascii`
  * Replace where HTTP whitespace semantics are intended.
  * Keep cookie OWS separate only if intentionally SP/TAB-only.
  * Value: tiny perf-neutral cleanup; prevents parser-policy drift.

* **Unify route pattern parsing/metadata**

  * App-side helpers:

    * `src/net/app_route_helpers.cxx:9` — validation
    * `src/net/app_route_helpers.cxx:47` — shape
    * `src/net/app_route_helpers.cxx:68` — param names
    * `src/net/app_route_helpers.cxx:95` — param types
  * Router-side helpers:

    * `src/net/router_match.cxx:16` — param-name logic
    * `src/net/router_match.cxx:24` — pattern parser
    * `src/net/router_match.cxx:124` — normalized pattern
  * Create one internal parsed-route-pattern representation.
  * Value: App validation, metadata, OpenAPI, router dispatch all agree.

* **Share OpenAPI rendering**

  * App renderer:

    * `src/net/app_openapi.cxx:71`
  * Router renderer:

    * `src/net/openapi.cxx:35`
  * Also duplicate JSON string escaping wrappers.
  * Make one internal OpenAPI route model + renderer; adapt App/Router route metadata into it.
  * Value: avoids two OpenAPI dialects.

* **DONE: Use one internal JSON string escaping wrapper**

  * Existing utility:

    * `src/utils.cxx:206` — `json_string_fallback`
    * `src/utils.cxx:229` — `append_json_string_fallback`
  * Duplicated wrappers:

    * `src/net/app_openapi.cxx:33`
    * `src/net/app_json_helpers.cxx:33`
    * `src/net/app_extractor_helpers.cxx:15`
    * `src/net/http_facade_problem.cxx:13`
    * `src/net/structured_log.cxx:31`
    * `src/net/openapi.cxx:35`
  * Prefer append-style internal helper for structured logs/problem JSON/OpenAPI.
  * Value: fewer temps, less duplicated fallback behavior.

* **Share request-body derived parsing between HTTP/1 and HTTP/2**

  * HTTP/1 assembly:

    * `src/net/http_server_dispatch.cxx:133`
    * `src/net/http_server_dispatch.cxx:267`
  * HTTP/2 assembly:

    * `src/net/http_server_h2.cxx:440`
  * Existing parsers:

    * `src/net/http_parse_helpers.cxx:16` — URL encoded
    * `src/net/http_server_helpers.cxx:342` — cookies
    * `src/net/http_server_helpers.cxx:467` — multipart
  * Add `populate_request_parts(...)` or similar internal helper.
  * Value: consistent query/form/cookie/file behavior across protocols.

## P2 — minor, only while nearby

* **DONE: Use common response factories for plain error responses**

  * Manual response construction:

    * `src/net/ip_filter.cxx:18`
    * `src/net/csrf.cxx:56`, `src/net/csrf.cxx:69`
    * `src/net/rate_limit.cxx:95`
    * `src/net/http_server_dispatch.cxx:168`
  * Existing helpers:

    * `src/net/response.cxx:292` onward
  * Add/route through internal `plain_error_response(status, body)` or public `Response::text`.
  * Value: less duplicated status/status_text/content_type.

* **Static-file forbidden/not-found HTML helpers**

  * Repeated static responses around:

    * `src/net/static_async.cxx:245`
    * `src/net/static_async.cxx:286`
    * `src/net/static_async.cxx:355`
    * `src/net/static_async.cxx:452`
  * Low impact; worthwhile only during static-file cleanup.

## Probably not worth touching

* **Raw syscalls in process/file-watch modules**

  * `src/process.cxx`, `src/file_watch.cxx`
  * These are implementation modules, not missed internal-use opportunities unless a broader fd/syscall wrapper policy is introduced.

* **Direct stderr writes in low-level runtime destructors/debug paths**

  * `src/work/carrier_coro.cxx`
  * `src/uring/uring_handle.cxx`
  * They likely avoid imports/allocations during fragile paths. Keep unless diagnostics policy changes.

DONE
